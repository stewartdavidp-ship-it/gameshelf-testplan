{
  "name": "gameshelf-test-server",
  "version": "1.0.0",
  "description": "Unified test runner for Game Shelf - runs Playwright from web UI, uses existing Firebase config",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "setup": "npm install && npx playwright install chromium && node setup.js",
    "setup-testplan": "node setup.js",
    "setup:all-browsers": "npm install && npx playwright install && node setup.js",
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "nodemon": "^3.0.2",
    "typescript": "^5.3.0"
  }
}
